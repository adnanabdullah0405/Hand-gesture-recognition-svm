# Hand-gesture-recognition-svm
Machine learning project that uses Support Vector Machine (SVM) to classify and recognize different hand gestures from images. Covers image preprocessing, feature extraction, model training, and evaluation for gesture-based recognition systems.
